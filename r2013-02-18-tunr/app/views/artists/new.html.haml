
.twelve.columns
  = render(:partial=>'shared/errors',:locals=>{:object=>@artist})
  = form_for @artist do |f|
    = f.text_field    :name
    = f.label         :name

    = f.text_field    :image
    = f.label         :image
    - Album.order(:name).each do |album|
      = check_box_tag('artist[album_ids][]', album.id, album.in?(@artist.albums.to_a))
      %span= album.name



    = f.submit

